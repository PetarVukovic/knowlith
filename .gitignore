/target
/knowlith/node_modules
/knowlith/dist

# Lake files. The company's own knowledge never belongs in the repo.
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Local machine settings.
.claude/settings.local.json

# Screenshots taken while testing.
*.png

# The daemon API secret, written per machine.
api.token

# Local tooling artifacts — not part of the product.
.codegraph/
knowlith/.playwright-mcp/
